# No hay dependencias externas requeridas para este proyecto
# Tkinter viene incluido con Python por defecto

# Sin embargo, si quieres agregar dependencias en el futuro, 
# puedes instalar con: pip install -r requirements.txt

# Ejemplo de dependencias que podrías agregar:
# pytest==7.4.0          # Para pruebas unitarias
# pillow==10.0.0         # Para manejo de imágenes
# pandas==2.0.3          # Para manejo de datos
# openpyxl==3.1.2        # Para exportar a Excel
